# API server URL — leave empty if dashboard is served from the same origin
VITE_API_URL=http://localhost:8080

# Admin key — must match ADMIN_API_KEY on the server
VITE_ADMIN_KEY=your-secret-admin-key
